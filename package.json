{
  "name": "zekoro-fun",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 5000 --local",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.0.0"
  }
}
