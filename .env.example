# Upstash Redis — get free at https://upstash.com
# Used to store incoming emails
UPSTASH_REDIS_REST_URL=https://YOUR_ENDPOINT.upstash.io
UPSTASH_REDIS_REST_TOKEN=YOUR_TOKEN

# Secret for webhook endpoint security (set any random string)
WEBHOOK_SECRET=your-webhook-secret-here
