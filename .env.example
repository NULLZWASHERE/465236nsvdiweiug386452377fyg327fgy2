# Get free Redis at https://upstash.com → Create Database → copy REST URL + token
UPSTASH_REDIS_REST_URL=https://YOUR_ENDPOINT.upstash.io
UPSTASH_REDIS_REST_TOKEN=YOUR_TOKEN

# Set any random string — used to secure the /api/webhook endpoint
WEBHOOK_SECRET=change-me
